export enum TableHeaderType {
  Text = 'text',
  Numeric = 'numeric',
  Boolean = 'boolean',
  Date = 'date',
  Enum = 'enum',
  Image = 'image',
  ApprovalState = 'ApprovalState',
  Rating = 'rating',
}
